query loadCourse($id: [String]) {
  courses(ids: $id) {
    id
    title
    schema
    units {
      id
      title
      answerable
    }
    users {
      id
      email
      role
      profile {
        displayName
      }
    }
  }
}
